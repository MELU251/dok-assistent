---
phase: 02-cicd-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/deploy.yml
autonomous: false
requirements:
  - CICD-01
  - CICD-02
  - CICD-03

must_haves:
  truths:
    - "A commit with a failing pytest results in a red CI status — no Docker image is built, no deploy runs"
    - "A successful workflow shows three separate green jobs in the GitHub Actions UI: test, build, deploy, each with visible needs: dependencies"
    - "After docker compose up -d, the deploy job polls the container health status and fails (non-zero exit) if the container is not healthy within 120 seconds"
    - "Tailscale connection failures in the deploy job are retried at least once before the job fails"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "Three-job CI/CD pipeline (test, build, deploy)"
      contains: "needs: [test]"
  key_links:
    - from: "test job"
      to: "build job"
      via: "needs: [test]"
      pattern: "needs:\\s*\\[test\\]"
    - from: "build job"
      to: "deploy job"
      via: "needs: [build]"
      pattern: "needs:\\s*\\[build\\]"
    - from: "deploy job"
      to: "VPS container"
      via: "docker inspect health loop over SSH"
      pattern: "docker inspect.*Health.Status"
---

<objective>
Restructure the single-job `build-and-deploy` workflow into three explicitly ordered GitHub Actions jobs (test, build, deploy) with Tailscale retry logic and a post-deploy container health check.

Purpose: A failing pytest must prevent build and deploy from running. Container startup failures on the VPS must surface in CI rather than silently pass.
Output: `.github/workflows/deploy.yml` rewritten as a three-job pipeline satisfying CICD-01, CICD-02, and CICD-03.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cicd-stabilization/02-RESEARCH.md

<interfaces>
<!-- Existing workflow structure to refactor. Extracted from .github/workflows/deploy.yml -->
<!-- Single job: build-and-deploy — all steps in one job, no test step, no needs: chain -->

Current job structure:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: { contents: read, packages: write, actions: write }
    steps:
      - actions/checkout@v4
      - tailscale/github-action@v2  (outdated — no ping: support)
      - nc -zv -w 10 VPS_HOST 22    (no retry)
      - SSH key setup
      - docker/setup-buildx-action@v3
      - docker/login-action@v3
      - docker/build-push-action@v5
      - scp docker-compose.yml
      - SSH deploy: write .env, docker compose pull, docker compose up -d
      # No health check step

Existing env var at workflow level:
  IMAGE: ghcr.io/melu251/dok-assistent:latest

Secrets used (all must remain):
  TAILSCALE_AUTH_KEY, VPS_HOST, VPS_SSH_KEY, VPS_USER,
  ANTHROPIC_API_KEY, OLLAMA_BASE_URL, OLLAMA_EMBED_MODEL,
  SUPABASE_URL, SUPABASE_SERVICE_KEY, CHUNK_SIZE, CHUNK_OVERLAP,
  TOP_K_RESULTS, LOG_LEVEL, CHAINLIT_AUTH_SECRET, CHAINLIT_USER,
  CHAINLIT_PASSWORD, GITHUB_TOKEN

docker-compose.yml healthcheck timing (existing):
  interval: 30s, timeout: 10s, retries: 3, start_period: 20s
  MAX_WAIT must be >= 120s (20 + 3*30 = 110s max Docker wait)

Container name: dok-assistent
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split workflow into test and build jobs (CICD-01, CICD-02)</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
Rewrite `.github/workflows/deploy.yml` completely. Keep the `on:` trigger and `env:` section unchanged. Replace the single `build-and-deploy` job with two new jobs: `test` and `build`.

**Job: test**
- `runs-on: ubuntu-latest`
- No `needs:` (entry point)
- Steps:
  1. `actions/checkout@v4`
  2. `actions/setup-python@v5` with `python-version: '3.11'`
  3. System dependencies: `sudo apt-get update -qq && sudo apt-get install -y --no-install-recommends libmagic1 poppler-utils tesseract-ocr` (required by unstructured.io at import time — Research Pitfall 4)
  4. Python dependencies: `pip install -r requirements.txt`
  5. Run tests: `pytest tests/ -v`
- NO secrets referenced in this job (tests use mocks only)

**Job: build**
- `needs: [test]` (CICD-01: only runs when test succeeds)
- `runs-on: ubuntu-latest`
- `permissions: { contents: read, packages: write, actions: write }` (packages: write needed for ghcr.io push; actions: write needed for GHA cache — Research Pitfall 2)
- Steps (identical to existing build steps):
  1. `actions/checkout@v4`
  2. `docker/setup-buildx-action@v3`
  3. `docker/login-action@v3` with ghcr.io + `${{ github.actor }}` + `${{ secrets.GITHUB_TOKEN }}`
  4. `docker/build-push-action@v5` with `context: .`, `push: true`, `tags: ${{ env.IMAGE }}`, `cache-from: type=gha`, `cache-to: type=gha,mode=min`

The `deploy` job is added in Task 2. Write only `test` and `build` jobs in this task.
  </action>
  <verify>
    <automated>pytest tests/ -v</automated>
  </verify>
  <done>deploy.yml contains `jobs: test:` with pytest step and `jobs: build:` with `needs: [test]`. Existing pytest suite passes locally (no regressions from workflow-only change).</done>
</task>

<task type="auto">
  <name>Task 2: Add deploy job with Tailscale retry and health check (CICD-03)</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
Add the `deploy` job to the workflow (after the `build` job):

**Job: deploy**
- `needs: [build]` (CICD-02: only runs when build succeeds)
- `runs-on: ubuntu-latest`
- `permissions: { contents: read }` (no packages: write needed)
- Steps:

1. `actions/checkout@v4`

2. Tailscale (upgrade from v2 to v4, add `ping:` — Research Pitfall 1):
```yaml
- name: Mit Tailscale verbinden
  uses: tailscale/github-action@v4
  with:
    authkey: ${{ secrets.TAILSCALE_AUTH_KEY }}
    args: --accept-routes --accept-dns
    ping: ${{ secrets.VPS_HOST }}
```

3. SSH key setup (identical to existing):
```yaml
- name: SSH-Key einrichten
  run: |
    mkdir -p ~/.ssh
    echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/deploy_key
    chmod 600 ~/.ssh/deploy_key
    ssh-keyscan -H ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts
```

4. VPS reachability with retry (CICD-03 — replaces bare `nc` with Wandalen/wretry.action):
```yaml
- name: VPS-Erreichbarkeit prüfen (mit Retry)
  uses: Wandalen/wretry.action@master
  with:
    command: nc -zv -w 10 ${{ secrets.VPS_HOST }} 22
    attempt_limit: 3
    attempt_delay: 10000
```

5. Copy files to VPS (identical to existing):
```yaml
- name: Dateien auf VPS kopieren
  run: |
    ssh -i ~/.ssh/deploy_key ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} \
      "mkdir -p /opt/dok-assistent"
    scp -i ~/.ssh/deploy_key \
      docker-compose.yml \
      ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}:/opt/dok-assistent/
```

6. Deploy on VPS (identical to existing — keep the exact quoting: outer `'ENDSSH'` prevents runner expansion, inner `EOF` allows secret interpolation — Research Pitfall 5):
```yaml
- name: Auf VPS deployen
  run: |
    ssh -i ~/.ssh/deploy_key ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} bash << 'ENDSSH'
      set -e
      cd /opt/dok-assistent
      cat > .env << EOF
    ANTHROPIC_API_KEY=${{ secrets.ANTHROPIC_API_KEY }}
    OLLAMA_BASE_URL=${{ secrets.OLLAMA_BASE_URL }}
    OLLAMA_EMBED_MODEL=${{ secrets.OLLAMA_EMBED_MODEL }}
    SUPABASE_URL=${{ secrets.SUPABASE_URL }}
    SUPABASE_SERVICE_KEY=${{ secrets.SUPABASE_SERVICE_KEY }}
    CHUNK_SIZE=${{ secrets.CHUNK_SIZE }}
    CHUNK_OVERLAP=${{ secrets.CHUNK_OVERLAP }}
    TOP_K_RESULTS=${{ secrets.TOP_K_RESULTS }}
    LOG_LEVEL=${{ secrets.LOG_LEVEL }}
    CHAINLIT_AUTH_SECRET=${{ secrets.CHAINLIT_AUTH_SECRET }}
    CHAINLIT_USER=${{ secrets.CHAINLIT_USER }}
    CHAINLIT_PASSWORD=${{ secrets.CHAINLIT_PASSWORD }}
    EOF
      echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u melu251 --password-stdin
      docker compose pull
      docker compose up -d --remove-orphans
    ENDSSH
```

7. Container health check (CICD-03 — new step, executes bash loop on VPS via SSH):
```yaml
- name: Container-Health-Check
  run: |
    ssh -i ~/.ssh/deploy_key ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} bash << 'ENDSSH'
      set -e
      MAX_WAIT=120
      ELAPSED=0
      CONTAINER=dok-assistent
      echo "Warte auf Container-Health-Check (max ${MAX_WAIT}s)..."
      while [ $ELAPSED -lt $MAX_WAIT ]; do
        STATUS=$(docker inspect --format='{{.State.Health.Status}}' $CONTAINER 2>/dev/null || echo "not_found")
        echo "  Status nach ${ELAPSED}s: $STATUS"
        if [ "$STATUS" = "healthy" ]; then
          echo "Container ist healthy."
          exit 0
        fi
        sleep 10
        ELAPSED=$((ELAPSED + 10))
      done
      echo "FEHLER: Container ist nach ${MAX_WAIT}s nicht healthy (Status: $STATUS)"
      exit 1
    ENDSSH
```

MAX_WAIT=120 is correct: Docker's full check window is 20s start_period + 3 retries * 30s interval = 110s maximum (Research Pitfall 3).
  </action>
  <verify>
    <automated>pytest tests/ -v</automated>
  </verify>
  <done>
deploy.yml has three jobs: `test`, `build` (needs: [test]), `deploy` (needs: [build]). Deploy job contains Tailscale v4 with ping:, Wandalen/wretry.action for nc check, and a 120s health-check bash loop. Existing pytest suite still passes (no Python changes).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify CI pipeline end-to-end</name>
  <files>.github/workflows/deploy.yml</files>
  <what-built>
Three-job GitHub Actions workflow: test, build, deploy. Tests gate builds. Deploy includes Tailscale retry and post-deploy container health check.
  </what-built>
  <action>Push the updated workflow and observe GitHub Actions results. See how-to-verify for exact steps.</action>
  <how-to-verify>
**Step 1 — Push and observe happy path:**
1. Commit and push the updated `deploy.yml` to master.
2. Open GitHub, navigate to Actions, open the most recent run.
3. Verify three separate job cards are visible: `test`, `build`, `deploy`.
4. Verify `build` and `deploy` each show a `needs:` dependency indicator in the UI.
5. Inspect the `deploy` job logs — confirm the line "Container ist healthy." appears at the end.

**Step 2 — Verify failing test blocks deploy (CICD-01):**
1. Temporarily add `assert False` to any test in `tests/` (e.g., top of `test_connection.py`).
2. Push to master.
3. Observe: `test` job turns red, `build` and `deploy` jobs are skipped (greyed out, not run).
4. Revert the broken assertion and push again.

**Expected outcome:**
- Happy path: three green jobs, health check log shows "Container ist healthy."
- Broken test: red `test` job, skipped `build` and `deploy` jobs.
  </how-to-verify>
  <verify>
    <automated>MISSING — verification requires live GitHub Actions run (see how-to-verify above)</automated>
  </verify>
  <done>GitHub Actions UI shows three green job cards (test, build, deploy) on a clean push. A broken-test push results in red test job and skipped build/deploy jobs.</done>
  <resume-signal>Type "approved" if both scenarios worked correctly, or describe any issues observed.</resume-signal>
</task>

</tasks>

<verification>
- `pytest tests/ -v` passes after every task (no regressions — no Python files changed)
- deploy.yml YAML syntax is valid (parseable by GitHub Actions)
- Three jobs present with correct `needs:` chain: test, build, deploy
- `tailscale/github-action@v4` used (not v2)
- Health check loop uses MAX_WAIT=120, polls every 10s, exits non-zero on timeout
- `Wandalen/wretry.action@master` wraps the `nc` reachability check with `attempt_limit: 3`
- No secrets referenced in the `test` job
- `permissions: packages: write` only on `build` job, not on `deploy` job
</verification>

<success_criteria>
1. Failing pytest produces red CI status, no Docker image built, no deploy triggered (CICD-01)
2. GitHub Actions UI shows three separate job cards with visible dependency chain (CICD-02)
3. Post-deploy health check polls container and fails loudly if not healthy within 120s; Tailscale SSH step retries 3 times before giving up (CICD-03)
</success_criteria>

<output>
After completion, create `.planning/phases/02-cicd-stabilization/02-01-SUMMARY.md`
</output>
