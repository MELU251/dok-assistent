---
phase: 08-ui-ux-polish
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_app_ui.py
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "tests/test_app_ui.py existiert und laedt ohne Importfehler"
    - "Alle vier Testklassen (TestWelcomeMessage, TestDokumenteCommand, TestGermanErrors, TestSourceCitations) sind als xfail markiert und schlagen im aktuellen Zustand fehl"
    - "pytest tests/test_app_ui.py -v laeuft ohne Syntaxfehler durch und meldet 4 xfail"
  artifacts:
    - path: "tests/test_app_ui.py"
      provides: "Testabdeckung fuer UI-01, UI-02, UI-03, UI-04"
      exports: ["TestWelcomeMessage", "TestDokumenteCommand", "TestGermanErrors", "TestSourceCitations"]
  key_links:
    - from: "tests/test_app_ui.py"
      to: "app.py"
      via: "import app + patch('app.*')"
      pattern: "patch\\(\"app\\."
---

<objective>
Testgeruest (Wave 0) fuer alle vier UI-Requirements erstellen.

Purpose: Vor der Implementierung die erwarteten Verhaltensweisen als ausfuehrbare xfail-Tests fixieren. Stellt sicher, dass die Implementierung in Plan 02 gegen klare Kriterien pruefbar ist.
Output: tests/test_app_ui.py mit vier Testklassen (je eine pro Requirement), alle xfail(strict=True).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-ux-polish/08-RESEARCH.md
@.planning/phases/08-ui-ux-polish/08-VALIDATION.md
</context>

<interfaces>
<!-- Relevante Exporte und Muster aus dem bestehenden Projekt -->

Aus tests/test_app_async.py (bestehendes Mocking-Pattern):
```python
import pytest
from unittest.mock import AsyncMock, MagicMock, patch

# asyncio_mode = auto (pytest.ini) — kein @pytest.mark.asyncio noetig
# Mocking-Pattern fuer cl.Message:
with patch("app.cl.Message") as mock_message:
    mock_message.return_value.send = AsyncMock(return_value=None)

# Mocking-Pattern fuer cl.user_session:
with patch("app.cl.user_session") as mock_session:
    mock_session.get.return_value = None

# on_message aufrufen:
msg = MagicMock()
msg.content = "/loeschen test.pdf"
await app.on_message(msg)
```

Aus app.py — relevante Signaturen:
```python
def _build_welcome_content() -> str: ...
async def on_message(message: cl.Message) -> None: ...
async def _run_rag_flow(question: str) -> None: ...
async def _run_upload_flow() -> None: ...
async def _run_delete_flow(filename: str) -> None: ...
async def _run_workflow_flow() -> None: ...
async def _run_workflow_generation(file_path, filename, angebot_data) -> None: ...
```

xfail(strict=True) Pattern aus Phase 03:
```python
@pytest.mark.xfail(strict=True, reason="Wave 0 stub — Implementierung fehlt noch")
async def test_something(self):
    assert False, "Nicht implementiert"
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testgeruest tests/test_app_ui.py mit xfail-Stubs erstellen</name>
  <files>tests/test_app_ui.py</files>
  <behavior>
    - TestWelcomeMessage::test_welcome_contains_upload_instructions — _build_welcome_content() enthaelt Text ueber das Hochladen von Dokumenten (UI-01)
    - TestWelcomeMessage::test_welcome_contains_question_instructions — _build_welcome_content() enthaelt Text ueber das Fragenstellen (UI-01)
    - TestWelcomeMessage::test_welcome_contains_system_limits — _build_welcome_content() enthaelt Text ueber Systemgrenzen / was das System nicht kann (UI-01)
    - TestDokumenteCommand::test_dokumente_command_lists_docs — on_message("/dokumente") sendet eine Nachricht die Dateinamen aus get_indexed_documents() enthaelt (UI-02)
    - TestDokumenteCommand::test_dokumente_command_empty — on_message("/dokumente") ohne indexierte Dokumente sendet eine Nachricht mit "keine" oder "Keine" (UI-02)
    - TestGermanErrors::test_rag_flow_error_no_internal_type — _run_rag_flow-Fehlerblock sendet keine Nachricht die "type(exc).__name__" oder den rohen Exception-Klassennamen enthaelt (UI-03)
    - TestGermanErrors::test_upload_flow_error_no_internal_type — _run_upload_flow-Fehlerblock (embed_and_store) sendet keine Nachricht mit "_Intern:" oder englischem Exception-Typ (UI-03)
    - TestGermanErrors::test_delete_flow_error_no_internal_type — _run_delete_flow-Fehlerblock sendet keine Nachricht mit "_Intern:" (UI-03)
    - TestGermanErrors::test_workflow_generation_error_no_internal_type — _run_workflow_generation-Fehlerblock sendet keine Nachricht mit "_Intern:" (UI-03)
    - TestSourceCitations::test_rag_flow_uses_cl_text_element — _run_rag_flow() mit vorhandenen Quellen ruft cl.Text auf (nicht nur cl.Message mit sources_block im content) (UI-04)
    - TestSourceCitations::test_cl_text_display_inline — cl.Text wird mit display="inline" aufgerufen (UI-04)
  </behavior>
  <action>
    Neue Datei tests/test_app_ui.py erstellen. Alle Tests als xfail(strict=True) markieren — sie MUESSEN im aktuellen Zustand fehlschlagen.

    Dateistruktur:
    ```python
    """Tests fuer UI/UX-Requirements UI-01 bis UI-04."""
    import pytest
    from unittest.mock import AsyncMock, MagicMock, patch


    class TestWelcomeMessage:
        """UI-01: Willkommensnachricht deckt alle drei Pflichtinhalte ab."""

        @pytest.mark.xfail(strict=True, reason="Wave 0 — UI-01 noch nicht implementiert")
        def test_welcome_contains_upload_instructions(self): ...

        @pytest.mark.xfail(strict=True, reason="Wave 0 — UI-01 noch nicht implementiert")
        def test_welcome_contains_question_instructions(self): ...

        @pytest.mark.xfail(strict=True, reason="Wave 0 — UI-01 noch nicht implementiert")
        def test_welcome_contains_system_limits(self): ...


    class TestDokumenteCommand:
        """UI-02: /dokumente-Befehl liefert aktuelle Dokumentenliste."""

        @pytest.mark.xfail(strict=True, reason="Wave 0 — UI-02 noch nicht implementiert")
        async def test_dokumente_command_lists_docs(self): ...

        @pytest.mark.xfail(strict=True, reason="Wave 0 — UI-02 noch nicht implementiert")
        async def test_dokumente_command_empty(self): ...


    class TestGermanErrors:
        """UI-03: Alle Fehlerpfade senden deutschen Text ohne technischen Jargon."""

        @pytest.mark.xfail(strict=True, reason="Wave 0 — UI-03 noch nicht implementiert")
        async def test_rag_flow_error_no_internal_type(self): ...

        # ... (weitere Fehlerpfade)


    class TestSourceCitations:
        """UI-04: Quellenangaben als cl.Text-Element mit display=inline."""

        @pytest.mark.xfail(strict=True, reason="Wave 0 — UI-04 noch nicht implementiert")
        async def test_rag_flow_uses_cl_text_element(self): ...

        @pytest.mark.xfail(strict=True, reason="Wave 0 — UI-04 noch nicht implementiert")
        async def test_cl_text_display_inline(self): ...
    ```

    Implementierung der Test-Bodies:
    - TestWelcomeMessage: Ruft `_build_welcome_content()` auf, prueft `assert "hochlad" in content.lower()`, `assert "frag" in content.lower()`, `assert "nicht" in content.lower() and ("enthalten" in content.lower() or "dokument" in content.lower())`
    - TestDokumenteCommand: Ruft `await app.on_message(msg)` mit `msg.content = "/dokumente"` auf; mockt `get_indexed_documents`, `cl.Message`, `cl.user_session`
    - TestGermanErrors: Loest Fehler aus (Mock wirft Exception), fang `cl.Message.call_args` ab, prueft dass "Intern:" NICHT im content vorkommt und kein roher Exception-Klassenname auftaucht
    - TestSourceCitations: Mockt `asyncio.to_thread` mit Ergebnis das `sources` enthaelt, prueft dass `cl.Text` aufgerufen wird und `display="inline"` uebergeben wird

    asyncio_mode = auto (pytest.ini) — kein `@pytest.mark.asyncio` noetig bei async-Methoden.

    Wichtig: xfail(strict=True) bedeutet: wenn der Test unexpectedly passed, wird er als FEHLER gemeldet. Das schuetzt gegen False Greens in Wave 0.
  </action>
  <verify>
    <automated>cd J:/OneDrive/Daten/Coding/RAGPoC && python -m pytest tests/test_app_ui.py -v 2>&amp;1 | tail -30</automated>
  </verify>
  <done>pytest tests/test_app_ui.py -v meldet ausschliesslich "xfail" fuer alle Tests (keine ERROR, keine FAILED, keine PASSED). Datei hat keine Importfehler.</done>
</task>

</tasks>

<verification>
pytest tests/test_app_ui.py -v zeigt: alle Tests als "xfail" markiert.
pytest tests/ -q -m "not integration" bleibt gruen (keine Regression).
</verification>

<success_criteria>
- tests/test_app_ui.py existiert mit 4 Testklassen und mindestens 11 Tests
- Alle Tests sind xfail(strict=True)
- pytest tests/test_app_ui.py -v zeigt 0 FAILED, 0 ERROR, 0 PASSED — nur xfail
- Die bestehende Testsuite bleibt unbeschraedigt
</success_criteria>

<output>
Nach Abschluss: .planning/phases/08-ui-ux-polish/08-01-SUMMARY.md erstellen.
</output>
