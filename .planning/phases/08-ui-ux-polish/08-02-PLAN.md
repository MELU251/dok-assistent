---
phase: 08-ui-ux-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - app.py
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "Willkommensnachricht erklaert: Dokument hochladen, Fragen stellen, was das System nicht kann"
    - "Nutzer kann jederzeit '/dokumente' tippen und erhaelt die aktuelle Liste indexierter Dateien"
    - "Kein Fehlertext an den Nutzer enthaelt '_Intern: ..._' oder rohe Python-Exception-Klassennamen"
    - "Quellenangaben erscheinen als cl.Text-Element mit display='inline' — nicht als Fliesstext in cl.Message.content"
    - "Alle bestehenden Tests bleiben gruen nach den Aenderungen"
  artifacts:
    - path: "app.py"
      provides: "UI-konforme Chat-Oberflaeche"
      contains: "/dokumente-Branch in on_message, cl.Text in _run_rag_flow, bereinigte Fehlertexte"
    - path: "tests/test_app_ui.py"
      provides: "Verifikation aller vier Requirements"
      exports: ["TestWelcomeMessage", "TestDokumenteCommand", "TestGermanErrors", "TestSourceCitations"]
  key_links:
    - from: "app.py on_message()"
      to: "get_indexed_documents()"
      via: "/dokumente-Branch mit startswith('/dokumente')"
      pattern: "startswith\\(\"/dokumente\"\\)"
    - from: "app.py _run_rag_flow()"
      to: "cl.Text"
      via: "elements=[cl.Text(name='Quellen', display='inline')]"
      pattern: "cl\\.Text.*display.*inline"
    - from: "app.py Fehlerblock _run_rag_flow"
      to: "cl.Message"
      via: "deutschsprachiger Text ohne type(exc).__name__"
      pattern: "(?!.*_Intern)"
---

<objective>
Alle vier UI/UX-Requirements in app.py implementieren und Tests gruen machen.

Purpose: Schließt die vier offenen UI-Gaps aus dem v1.0-Audit. Alle Aenderungen sind auf app.py beschraenkt — kein neues Modul, keine neuen Abhaengigkeiten.
Output: app.py mit /dokumente-Befehl, erweiterter Willkommensnachricht, bereinigten Fehlertexten und visuellen Quellenangaben als cl.Text-Element.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-ui-ux-polish/08-RESEARCH.md
@.planning/phases/08-ui-ux-polish/08-VALIDATION.md
@.planning/phases/08-ui-ux-polish/08-01-SUMMARY.md
</context>

<interfaces>
<!-- Relevante aktuelle Zustaende aus app.py die geaendert werden -->

_build_welcome_content() — aktuell (unvollstaendig, UI-01 partial):
```python
def _build_welcome_content() -> str:
    docs = get_indexed_documents()
    # ...
    return (
        "## Willkommen beim Dokumenten-Assistenten\n\n"
        "Ich beantworte Ihre Fragen direkt aus Ihren Unterlagen.\n\n"
        # Fehlt: Upload-Erklaerung, Fragen-Erklaerung, Systemgrenzen
        "- Klicken Sie **Dokument hochladen** ...\n"
        "- Tippen Sie `/loeschen [dateiname]` ..."
        # Fehlt: /dokumente in der Befehlsliste
    )
```

on_message() — aktuell (UI-02: kein /dokumente-Branch):
```python
async def on_message(message: cl.Message) -> None:
    text = message.content.strip()
    if text.lower().startswith("/filter"): ...   # Branch 1
    if text.lower().startswith("/loeschen"): ... # Branch 2
    await _run_rag_flow(text)                    # Fallback — /dokumente fehlt hier
```

_run_rag_flow() — Fehlerblock (UI-03 verletzt):
```python
except Exception as exc:
    await cl.Message(content=(
        "Bei der Verarbeitung ist ein Fehler aufgetreten.\n\n"
        "Bitte pruefen Sie die Verbindung und versuchen Sie es erneut.\n"
        f"_(Intern: {type(exc).__name__})_"  # <-- verletzt UI-03
    )).send()
```

_run_rag_flow() — Quellenangaben (UI-04 verletzt):
```python
sources_block = ""
if result["sources"]:
    source_lines = "\n".join(f"- {s}" for s in result["sources"])
    sources_block = f"\n\n---\n**Quellen:**\n{source_lines}"
# Dann: cl.Message(content=result["answer"] + sources_block + cost_note).send()
# Fehlt: cl.Text(display="inline") als Element
```

_run_upload_flow() — Fehlerblock (UI-03 verletzt):
```python
f"Details (intern): {type(exc).__name__}"  # <-- verletzt UI-03
```

_run_delete_flow() — Fehlerblock (UI-03 verletzt):
```python
content=f"Datei wurde geloescht, aber Datenbankfehler: {type(exc).__name__}"
```

_run_workflow_flow() — Fehlerblock (UI-03 verletzt):
```python
f"_(Intern: {type(exc).__name__})_"
```

_run_workflow_generation() — Fehlerblock (UI-03 verletzt):
```python
f"_(Intern: {type(exc).__name__})_"
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Willkommensnachricht (UI-01) und /dokumente-Befehl (UI-02) implementieren</name>
  <files>app.py</files>
  <behavior>
    - _build_welcome_content() enthaelt Text ueber Upload-Moeglichkeit (Schluesselwort: "hochlad")
    - _build_welcome_content() enthaelt Text ueber Fragen stellen (Schluesselwort: "frag")
    - _build_welcome_content() enthaelt Text ueber Systemgrenzen ("nicht" + "enthalten" oder "Dokument")
    - _build_welcome_content() listet /dokumente als Befehl auf
    - on_message("/dokumente") mit indexierten Dokumenten sendet Nachricht die Dateinamen enthaelt
    - on_message("/dokumente") ohne Dokumente sendet Nachricht mit "keine" oder "Keine"
    - /dokumente-Branch schliesst mit return (kein Fall-through)
  </behavior>
  <action>
    Zwei Aenderungen an app.py:

    **1. _build_welcome_content() erweitern (UI-01 + /dokumente in Befehlsliste):**

    Den Rueckgabe-String ersetzen durch:
    ```python
    return (
        "## Willkommen beim Dokumenten-Assistenten\n\n"
        "Ich beantworte Ihre Fragen direkt aus Ihren Unterlagen — "
        "mit Quellenangabe (Dateiname + Seite).\n\n"
        f"{library_section}\n\n"
        "---\n"
        "**So funktioniert es:**\n"
        "- **Dokument hochladen:** Klicken Sie _Dokument hochladen_ — "
        "unterstuetzt PDF, DOCX und XLSX (max. 50 MB)\n"
        "- **Fragen stellen:** Tippen Sie einfach Ihre Frage — "
        "ich durchsuche alle indexierten Dokumente\n"
        "- **Wichtig:** Ich beantworte nur Fragen auf Basis der hochgeladenen Dokumente. "
        "Informationen die nicht in Ihren Unterlagen stehen, kann ich nicht liefern.\n\n"
        "**Befehle:**\n"
        "- `/dokumente` — aktuelle Dokumentenliste anzeigen\n"
        "- `/filter [dateiname]` — Suche auf ein bestimmtes Dokument eingrenzen\n"
        "- `/loeschen [dateiname]` — Dokument vollstaendig entfernen\n"
    )
    ```

    **2. /dokumente-Branch in on_message() einfuegen (UI-02):**

    Nach dem /loeschen-Block, VOR dem abschliessenden `await _run_rag_flow(text)`:
    ```python
    # Dokumentenliste: /dokumente
    if text.lower().startswith("/dokumente"):
        docs = get_indexed_documents()
        if docs:
            doc_list = "\n".join(f"  - {d}" for d in docs)
            content = f"**Aktuell indexierte Dokumente ({len(docs)}):**\n{doc_list}"
        else:
            content = "_Keine Dokumente indexiert. Laden Sie ein Dokument hoch._"
        await cl.Message(content=content).send()
        return
    ```

    Der /dokumente-Branch muss mit return abschliessen (analog zu /filter und /loeschen).
    Die Reihenfolge der Branches: /filter -> /loeschen -> /dokumente -> RAG-Fallback.
  </action>
  <verify>
    <automated>cd J:/OneDrive/Daten/Coding/RAGPoC && python -m pytest tests/test_app_ui.py::TestWelcomeMessage tests/test_app_ui.py::TestDokumenteCommand -v 2>&amp;1 | tail -20</automated>
  </verify>
  <done>TestWelcomeMessage und TestDokumenteCommand zeigen alle Tests als PASSED. Kein FAILED, kein ERROR.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fehlertext-Bereinigung (UI-03) und visuelle Quellenangaben (UI-04)</name>
  <files>app.py</files>
  <behavior>
    - _run_rag_flow-Fehlerblock: cl.Message.content enthaelt NICHT "_Intern:" und NICHT den rohen Exception-Klassennamen
    - _run_upload_flow-Fehlerblock (embed_and_store): cl.Message.content enthaelt NICHT "Details (intern):" oder "_Intern:"
    - _run_delete_flow-Fehlerblock: cl.Message.content enthaelt NICHT "type(exc).__name__" oder "_Intern:"
    - _run_workflow_flow-Fehlerblock (Extraktion): cl.Message.content enthaelt NICHT "_Intern:"
    - _run_workflow_generation-Fehlerblock: cl.Message.content enthaelt NICHT "_Intern:"
    - _run_rag_flow() mit nicht-leeren sources: cl.Text wird mit name="Quellen" und display="inline" aufgerufen
    - cl.Message wird mit elements=[...] aufgerufen (nicht mehr sources_block im content)
    - _run_rag_flow() ohne sources: cl.Text wird nicht aufgerufen
  </behavior>
  <action>
    **1. UI-03: Alle _(Intern: ...)_ und type(exc).__name__ aus User-sichtbaren Messages entfernen.**

    Grep-Kandidaten in app.py (alle fuenf Stellen):

    In _run_upload_flow() — ersetzen:
    ```python
    # ALT: f"Details (intern): {type(exc).__name__}"
    # NEU:
    "Bitte pruefen Sie die Verbindung (Ollama/Supabase) und versuchen Sie es erneut."
    ```

    In _run_delete_flow() — ersetzen:
    ```python
    # ALT: f"Datei wurde geloescht, aber Datenbankfehler: {type(exc).__name__}"
    # NEU:
    "Die Datei wurde entfernt, aber beim Bereinigen der Datenbank ist ein Fehler aufgetreten.\n"
    "Bitte versuchen Sie es erneut oder pruefen Sie die Supabase-Verbindung."
    ```

    In _run_rag_flow() — entfernen:
    ```python
    # ALT: f"_(Intern: {type(exc).__name__})_"
    # NEU: Zeile ersatzlos streichen — der vorherige Satz genuegt
    "Bei der Verarbeitung ist ein Fehler aufgetreten.\n\n"
    "Bitte pruefen Sie die Verbindung und versuchen Sie es erneut."
    ```

    In _run_workflow_flow() (Extraktion-Fehler) — entfernen:
    ```python
    # ALT: f"_(Intern: {type(exc).__name__})_"
    # NEU: Zeile ersatzlos streichen
    "Die Anforderungsextraktion ist fehlgeschlagen.\n"
    "Bitte pruefen Sie die Verbindung und versuchen Sie es erneut."
    ```

    In _run_workflow_generation() — entfernen:
    ```python
    # ALT: f"_(Intern: {type(exc).__name__})_"
    # NEU: Zeile ersatzlos streichen
    "Fehler bei der Entwurfs-Generierung.\n"
    "Bitte pruefen Sie die Verbindung und versuchen Sie es erneut."
    ```

    logger.error(...) darf weiterhin exc und type(exc).__name__ enthalten — nur cl.Message.content muss bereinigt werden.

    **2. UI-04: Quellenangaben als cl.Text-Element in _run_rag_flow().**

    Den sources_block-Ansatz vollstaendig ersetzen:
    ```python
    # NEU (ersetzt sources_block + altes cl.Message-send):
    elements = []
    if result["sources"]:
        sources_text = "\n".join(f"- {s}" for s in result["sources"])
        elements.append(
            cl.Text(
                name="Quellen",
                content=f"**Quellen:**\n{sources_text}",
                display="inline",
            )
        )
    cost_cent = result["cost_eur"] * 100
    cost_note = f"\n\n<sub>Kosten dieser Anfrage: ~{cost_cent:.3f} Cent</sub>"
    await cl.Message(
        content=result["answer"] + cost_note,
        elements=elements,
    ).send()
    ```

    Nach allen Aenderungen: `python -c "import app"` darf keinen Fehler werfen.
  </action>
  <verify>
    <automated>cd J:/OneDrive/Daten/Coding/RAGPoC && python -m pytest tests/test_app_ui.py -v 2>&amp;1 | tail -25</automated>
  </verify>
  <done>pytest tests/test_app_ui.py -v zeigt alle Tests als PASSED. pytest tests/ -q -m "not integration" bleibt gruen.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visuelle Verifikation im Browser</name>
  <action>Manuell pruefen: Willkommensnachricht, /dokumente-Befehl, Quellenangaben-Rendering und Fehlertext-Qualitaet im laufenden Chainlit.</action>
  <verify>Visuelle Pruefung im Chainlit-Browser — alle vier Requirements visuell bestaetigt.</verify>
  <done>Alle vier Punkte (UI-01 Willkommensnachricht, UI-02 /dokumente, UI-03 deutsche Fehler, UI-04 cl.Text Quellen) im Browser bestaetigt.</done>
  <what-built>
    Alle vier UI-Requirements in app.py implementiert:
    - UI-01: Willkommensnachricht mit Upload-Erklaerung, Fragen-Erklaerung, Systemgrenzen
    - UI-02: /dokumente-Befehl zeigt aktuelle Dokumentenliste jederzeit im Chat
    - UI-03: Alle Fehlerpfade senden deutschen Text ohne technischen Jargon
    - UI-04: Quellenangaben als cl.Text-Element mit display="inline"
  </what-built>
  <how-to-verify>
    1. App starten: `chainlit run app.py` (oder via Docker)
    2. Neuen Browser-Tab oeffnen (neue Session):
       - Willkommensnachricht pruefen: enthaelt Upload-Erklaerung, Fragen-Erklaerung und "nicht in den Dokumenten"
    3. Im Chat `/dokumente` tippen:
       - Wenn Dokumente vorhanden: Liste erscheint mit Dateinamen
       - Wenn keine Dokumente: Meldung "Keine Dokumente indexiert"
    4. Eine RAG-Frage stellen (Dokument muss indexiert sein):
       - Quellenangabe erscheint als visuell abgesetzter Block (cl.Text inline) — nicht als Fliesstext
    5. Optional: Fehler provozieren (Tailscale deaktivieren, dann Upload versuchen):
       - Fehlermeldung erscheint auf Deutsch ohne Exception-Typen wie "RuntimeError"
  </how-to-verify>
  <resume-signal>Tippen Sie "approved" wenn alle Punkte korrekt aussehen, oder beschreiben Sie Probleme.</resume-signal>
</task>

</tasks>

<verification>
Automatisch: pytest tests/test_app_ui.py -v — alle 11+ Tests PASSED.
Automatisch: pytest tests/ -q -m "not integration" — keine Regressionen.
Manuell: Chainlit-Session — Willkommensnachricht, /dokumente, Quellenangaben-Rendering.
</verification>

<success_criteria>
- pytest tests/test_app_ui.py -v: 0 FAILED, 0 ERROR (alle PASSED)
- pytest tests/ -q -m "not integration": gruen, keine neuen Fehler
- app.py enthaelt keinen String "_Intern:" in cl.Message-content-Konstruktionen
- app.py enthaelt keinen String "type(exc).__name__" in cl.Message-content-Konstruktionen (nur in logger.error erlaubt)
- _run_rag_flow() verwendet cl.Text(display="inline") fuer Quellenangaben
- on_message() hat /dokumente-Branch der mit return abschliesst
- _build_welcome_content() erklaert Upload, Fragenstellen und Systemgrenzen
</success_criteria>

<output>
Nach Abschluss: .planning/phases/08-ui-ux-polish/08-02-SUMMARY.md erstellen.
</output>
